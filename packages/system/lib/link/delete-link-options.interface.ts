import { FileSystemEntryOptions } from '../file-system-entry';

/**
 * DeleteLinkOptions
 * 
 * Options for deleting a symbolic link.
 */

export interface DeleteLinkOptions extends FileSystemEntryOptions {
}