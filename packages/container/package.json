{
  "name": "@swindle/container",
  "version": "1.0.0",
  "description": "A simple dependency injection container for TypesScript.",
  "main": "dist/index.js",
  "repository": "https://github.com/Perivel/swindle",
  "author": "Patrick Luy <pluy@perivel.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn clean && tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "inspect:circular": "npx madge --circular --extensions ts ./",
    "clean": "rimraf ./dist",
    "lint": "tslint --fix -c tslint.json 'src/**/*.ts'"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "keywords": [
    "dic",
    "di",
    "dependency injection container",
    "dependency injection",
    "dependency inversion",
    "inversion of control container"
  ]
}
